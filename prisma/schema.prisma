datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Person {
  id Int @id @default(autoincrement())
  personId String @unique
  firstName String
  lastName String
  email String?
  quickNote String?
}
